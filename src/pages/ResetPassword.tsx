import { FunctionComponent } from "react";
import ResetPasswordDetails from "../components/ResetPasswordDetails";

const ResetPassword: FunctionComponent = () => {
  return <ResetPasswordDetails />;
};

export default ResetPassword;
